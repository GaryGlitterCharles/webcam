node {  
    stage('Build') { 
        def HOST = 'THE'
        def BASEDIR = 'BEST'
        def NPM_COMAND = 'CHAR'
        echo "${HOST}"
        echo "${BASEDIR}"
        echo "${NPM_COMAND}"
      phile('diwal')
    }
}
def phile(test) {
  echo "${test}"
  
}
